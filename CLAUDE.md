# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty — there is no source code, build configuration, test suite, or README. There is nothing project-specific to document yet (no build/lint/test commands, no architecture, no conventions).

## What to do on the first real change

When the first real code or configuration lands in this repo, replace this file with a proper CLAUDE.md. At that point, populate it with:

- The actual build, lint, test, and run commands (including how to run a single test) once a toolchain is chosen.
- The high-level architecture once there is more than one module — focus on the cross-file "big picture" that isn't obvious from reading a single file.
- Any conventions established in a README, `.cursor/rules/`, `.cursorrules`, or `.github/copilot-instructions.md` if/when they are added.

Do not invent commands or architecture before they exist in the repo.

## Development branch

Per the active task instructions, ongoing documentation work happens on branch `claude/add-claude-documentation-wXp5W`.
